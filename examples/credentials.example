# Example S3-compatible credential file for local demos only.
# Copy to a safe location outside the repo and replace every value.

[demo-provider]
endpoint = https://your-object-storage.example.com
access_key = YOUR_ACCESS_KEY_ID
secret_key = YOUR_SECRET_ACCESS_KEY
region = eu-central-1
